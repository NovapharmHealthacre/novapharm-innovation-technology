{
  "name": "novapharm-innovation-technology-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free static website for Novapharm Innovation Technology.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate-site.mjs",
    "syntax": "node --check assets/js/site.js && node --check scripts/validate-site.mjs",
    "check": "npm run syntax && npm run validate"
  }
}
